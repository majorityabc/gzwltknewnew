# 物理题库管理系统开发记录

## 项目概述

开发一个高中物理题库管理系统，核心功能：
1. 从 Word 复制题目并原样显示（包含公式、图片、表格、格式）
2. 多维度标签管理（章节、知识点、难度、题型）
3. 灵活的筛选功能
4. Word 文档导出

## 技术栈

### 后端
- Node.js + Express + TypeScript
- SQLite + Prisma ORM
- docx.js（Word 导出）
- multer（文件上传）

### 前端
- React 18 + TypeScript
- Ant Design 5（UI 组件）
- TinyMCE（富文本编辑器）
- Zustand（状态管理）
- Vite（构建工具）

## 开发过程

### 第一阶段：项目搭建

1. **创建项目结构**
   - 前端目录：`physics-question-bank/frontend`
   - 后端目录：`physics-question-bank/backend`

2. **后端初始化**
   - 配置 package.json 和 TypeScript
   - 设计数据库模型（Prisma Schema）
   - 创建 API 路由（题目、章节、知识点、标签、上传、导出）

3. **前端初始化**
   - 配置 Vite + React + TypeScript
   - 集成 Ant Design
   - 创建基础组件和页面

4. **数据库配置**
   - 使用 SQLite（无需安装数据库服务器）
   - 运行 Prisma 迁移初始化数据库
   - 生成 Prisma Client

### 第二阶段：核心功能实现

1. **题目管理 API**
   - GET /api/questions - 获取题目列表（支持筛选）
   - GET /api/questions/:id - 获取单个题目
   - POST /api/questions - 创建题目
   - PUT /api/questions/:id - 更新题目
   - DELETE /api/questions/:id - 删除题目

2. **标签管理 API**
   - 章节管理（支持层级结构）
   - 知识点管理
   - 自定义标签管理

3. **图片上传**
   - 支持拖拽上传
   - 自动生成唯一文件名
   - 保存到本地 uploads 目录

4. **Word 导出**
   - 使用 docx.js 生成 Word 文档
   - 保留题目格式和标签信息
   - 支持批量导出

5. **前端界面**
   - Dashboard 主页面
   - QuestionList 题目列表（支持分页、选择）
   - QuestionFilter 筛选面板
   - QuestionForm 题目编辑表单
   - QuestionEditor 富文本编辑器

### 第三阶段：富文本编辑器优化

**问题：** 初始使用 React Quill，但对 Word 粘贴支持不好，编辑器显示异常。

**解决方案：**
1. 更换为 TinyMCE 编辑器（更好的 Word 粘贴支持）
2. 配置 TinyMCE 资源文件到 public 目录
3. 启用 paste 插件和图片自动上传

**端口配置：**
- 后端：http://localhost:3002
- 前端：http://localhost:3000

## 当前状态

### 已完成功能
✅ 项目基础架构
✅ 数据库设计和初始化
✅ 后端 API（CRUD、筛选、上传、导出）
✅ 前端界面（列表、筛选、表单）
✅ 富文本编辑器集成
✅ 图片上传功能
✅ Word 导出功能
✅ 标签系统（章节、知识点、难度、题型）

### 待解决问题

**核心问题：Word 公式粘贴**

从 Word 直接粘贴公式到网页编辑器是技术难题，因为：
- Word 使用 OMML 格式存储公式
- 网页需要 MathML 或 LaTeX 格式
- 浏览器无法自动转换格式

**三个解决方案：**

1. **方案 1：截图粘贴（推荐，立即可用）**
   - 在 Word 中对公式截图
   - 粘贴图片到编辑器
   - 优点：简单直接，所见即所得
   - 缺点：公式是图片，不能编辑

2. **方案 2：使用编辑器格式功能**
   - 使用上标/下标按钮
   - 手动输入公式
   - 适合简单公式（如 x²、H₂O）

3. **方案 3：集成专业公式编辑器**
   - 集成 MathJax 或 KaTeX
   - 支持 LaTeX 语法输入
   - 需要 1-2 天开发时间
   - 可以实现公式编辑和渲染

## 使用说明

### 启动系统

1. **启动后端**
```bash
cd F:/claudetiku/physics-question-bank/backend
npm run dev
```
后端运行在 http://localhost:3002

2. **启动前端**
```bash
cd F:/claudetiku/physics-question-bank/frontend
npm run dev
```
前端运行在 http://localhost:3000

### 使用流程

1. 访问 http://localhost:3000
2. 点击"新建题目"
3. 在 TinyMCE 编辑器中输入或粘贴题目内容
4. 对于公式：
   - 临时方案：截图后粘贴
   - 或使用上标/下标按钮手动输入
5. 选择章节、知识点、难度、题型
6. 保存题目
7. 使用左侧筛选面板进行筛选
8. 勾选题目后点击"导出 Word"

## 项目文件结构

```
physics-question-bank/
├── backend/
│   ├── src/
│   │   ├── routes/
│   │   │   ├── questions.ts      # 题目 API
│   │   │   ├── chapters.ts       # 章节 API
│   │   │   ├── knowledgePoints.ts # 知识点 API
│   │   │   ├── tags.ts           # 标签 API
│   │   │   ├── upload.ts         # 上传 API
│   │   │   └── export.ts         # 导出 API
│   │   └── index.ts              # 入口文件
│   ├── prisma/
│   │   └── schema.prisma         # 数据库模型
│   ├── .env                      # 环境变量
│   └── package.json
│
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── QuestionEditor.tsx    # 富文本编辑器
│   │   │   ├── QuestionForm.tsx      # 题目表单
│   │   │   ├── QuestionList.tsx      # 题目列表
│   │   │   └── QuestionFilter.tsx    # 筛选面板
│   │   ├── pages/
│   │   │   └── Dashboard.tsx         # 主页面
│   │   ├── services/
│   │   │   └── api.ts                # API 服务
│   │   ├── stores/
│   │   │   └── questionStore.ts      # 状态管理
│   │   ├── App.tsx
│   │   └── main.tsx
│   ├── public/
│   │   └── tinymce/                  # TinyMCE 资源
│   └── package.json
│
├── README.md
├── INSTALL.md
└── 高中物理题库管理系统-技术方案.md
```

## 数据库设计

### 主要表结构

1. **questions** - 题目表
   - id, content, plainText, questionType, difficulty, chapterId
   - 关联：章节、知识点、标签、图片

2. **chapters** - 章节表
   - id, name, parentId, sortOrder
   - 支持层级结构

3. **knowledge_points** - 知识点表
   - id, name, chapterId, description

4. **tags** - 标签表
   - id, name, color

5. **question_knowledge_points** - 题目-知识点关联表（多对多）

6. **question_tags** - 题目-标签关联表（多对多）

7. **images** - 图片表
   - id, questionId, url, filename, size

## 下一步计划

### 短期优化
1. 完善公式支持（选择方案并实施）
2. 添加更多预设章节和知识点
3. 优化编辑器体验
4. 改进 Word 导出格式

### 中期功能
1. 批量导入题目
2. 题目去重检测
3. 数据备份和恢复
4. 搜索功能优化

### 长期扩展
1. 用户系统和权限管理
2. 团队协作功能
3. 移动端适配
4. 云端部署

## 技术难点总结

### 1. Word 内容粘贴
- **难点**：格式转换、公式识别
- **当前方案**：TinyMCE 基础粘贴 + 图片截图
- **理想方案**：OMML → MathML 转换

### 2. 公式编辑和显示
- **难点**：网页公式渲染
- **可选方案**：MathJax、KaTeX、图片
- **权衡**：易用性 vs 功能完整性

### 3. Word 导出
- **难点**：HTML → DOCX 转换，保留格式
- **当前方案**：docx.js 基础导出
- **待优化**：公式、图片、表格的完整支持

## 开发心得

1. **技术选型很重要**：从 React Quill 换到 TinyMCE 解决了很多问题
2. **数据库选择**：SQLite 简化了部署，适合个人使用
3. **渐进式开发**：先实现核心功能，再逐步优化
4. **用户需求优先**：公式问题虽然复杂，但可以用简单方案先解决

## 参考资源

- TinyMCE 文档：https://www.tiny.cloud/docs/
- Prisma 文档：https://www.prisma.io/docs/
- docx.js 文档：https://docx.js.org/
- Ant Design 文档：https://ant.design/

---

**项目状态：** MVP 已完成，可以使用
**最后更新：** 2026-04-24
